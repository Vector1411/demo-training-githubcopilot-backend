# Copy this file to .env and fill the values (do NOT commit your real credentials)
# Two options to provide Firebase Admin credentials:
# 1) Use a service account JSON file and set GOOGLE_APPLICATION_CREDENTIALS to its path
#    (recommended for local development and CI). Example:
#    GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
#
# 2) Or paste service account fields into env vars below (less secure). When
#    pasting the private key into an env var, replace literal newlines with \n
#    OR keep the JSON file and use GOOGLE_APPLICATION_CREDENTIALS.

# Option A (preferred):
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/your-service-account.json

# Option B (explicit fields):
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# When pasting the private key into .env, convert actual newlines to \n
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----\n"

# App settings
PORT=3001
JWT_SECRET=change-me
JWT_EXPIRES_IN=3600

# Notes:
# - To create a service account JSON: go to Firebase Console -> Project Settings -> Service accounts
#   -> Generate new private key. Save the JSON file somewhere secure and point
#   GOOGLE_APPLICATION_CREDENTIALS to it, or copy the values into the FIREBASE_* vars.
# - Never commit your private key or .env with secrets. Use secrets manager for production.
